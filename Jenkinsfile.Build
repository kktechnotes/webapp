node {
        stage ('Initialize') {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${MVN_test}"
                ''' 
        }

        stage ('Build') {
                sh 'mvn clean package'
        }
}
